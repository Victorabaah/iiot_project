apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Edit if your workloads deploy into another namespace
namespace: edge-system

resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml
  - serviceaccount.yaml

commonLabels:
  app.kubernetes.io/name: edge-app
  app.kubernetes.io/part-of: iot-automation
  app.kubernetes.io/component: edge-runtime
