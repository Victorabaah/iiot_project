apiVersion: v1
kind: Service
metadata:
  name: edge-app
spec:
  selector:
    app: edge-app
  ports:
    - name: http
      port: 80
      # Change if container port differs
      targetPort: 8080
  type: ClusterIP
